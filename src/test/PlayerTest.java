// DA FARE PER USER E CPU (player è abstract)

/*package test;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import model.Player;
import model.User;

import static org.junit.jupiter.api.Assertions.*;

public class PlayerTest {
	private Player player;

	@BeforeEach
	void setUp() {
		player = new User("John");
	}

	@Test
	public void testPlayerName() {
		assertEquals("John", player.getName(), "Player name should match the initialized value.");
	}


	@Test
	public void testPlayerIncreaseScore() {
		player.increaseScore(10);
		player.increaseScore(5);
		assertEquals(15, player.getScore(), "Player score should reflect the added value.");
	}
}
*/
